- Save each list in the form of a txt file.
- The  name for the file will also the identifier.

- The structure of list:
+ The tasks
+ The number of tasks

-The structure of a task:
+ The content
+ The status

- Use command to modify the list
:tick + number : to tick off the <number> element in the list.
:w

# COMMAND INTERPRETER
The command interpreter needs to read the input, parse the input, modify the list based on the command.
Need to have access to the list
=> Make the list an attribute of the interpreter

Step 1: Create the structure of a task and a list.
Step 2: A command interpreter to understand the command.
Step 3: A way to save the list under the form of a txt file.
Step 4: Design command to interact with the list.

Operation:
NAME			COMMAND			OPERAND			INDEX
add			:add			taskName		0
remove			:remove			index			1		
tickOff			:tick			index			2
change			:rename			index	taskName	3
setFileName		:setFileName		fileName		4
open			:open			fileName		5
close			:close						6

Structure of the file
- FileName
- Status of the task + Content of the task
+ 0: untick	1 : tick

